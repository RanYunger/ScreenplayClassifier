<UserControl x:Class="ScreenplayClassifier.MVVM.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views">
    <Grid>
        <Image x:Name="FolderImage" MaxHeight="200" MaxWidth="200" Cursor="Hand"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="80" MaxWidth="80" Cursor="Hand">
            <Image x:Name="GenreImage"/>
            <TextBlock x:Name="ScreenplaysCountTextBlock" FontSize="15" Margin="0,10,0,0" Style="{StaticResource TextBlockStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
