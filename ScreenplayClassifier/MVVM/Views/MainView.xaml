<Window x:Class="ScreenplayClassifier.MVVM.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        Title="Screenplay Classifier" MinHeight="700" MinWidth="1210" WindowState="Maximized" SizeToContent="WidthAndHeight"
        Style="{StaticResource WindowStyle}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <i:InvokeCommandAction Command="{Binding CheckKeyCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <v:UserToolbarView x:Name="UserToolbarView" Grid.Row="0"/>
        <StackPanel Grid.Row="1">
            <!--<TextBlock Text="TEMPLATE" FontSize="70" Style="{StaticResource TextBlockStyle}"/>-->
            <v:MenuView x:Name="MenuView"/>
        </StackPanel>
    </Grid>
</Window>
