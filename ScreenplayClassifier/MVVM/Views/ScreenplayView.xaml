<UserControl x:Class="ScreenplayClassifier.MVVM.Views.ScreenplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
             Style="{StaticResource UserControlStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Path=Screenplay.Title, StringFormat=Title: {0}}" HorizontalAlignment="Center" FontSize="50"
                   Margin="10" Style="{StaticResource TextBlockStyle}"/>
        <v:ScreenplayOverviewView x:Name="ScreenplayOverviewView" Grid.Row="1"/>
        <v:ScreenplayInspectionView x:Name="ScreenplayInspectionView" Grid.Row="1" Visibility="Collapsed"/>
    </Grid>
</UserControl>
