<UserControl x:Class="ScreenplayClassifier.MVVM.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
             Style="{StaticResource UserControlStyle}">
    <!--TODO: FIX (COMMAND NOT CALLED)-->
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <i:InvokeCommandAction Command="{Binding CheckKeyCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Welcome to Screenplay Classifier" HorizontalAlignment="Center" FontSize="70"
                       Style="{StaticResource TextBlockStyle}"/>
        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="{Binding LeftImage}" Height="300" Width="300" Margin="50,-50,50,50"/>
            <StackPanel ToolTip="{Binding ModuleTooltip}" Margin="50">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDown">
                        <i:InvokeCommandAction Command="{Binding ShowModuleViewCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Image Source="{Binding CenterImage}" Height="400" Width="400" Cursor="Hand" Margin="50,0,50,0"/>
                <TextBlock Text="{Binding CenterModule}" HorizontalAlignment="Center" FontSize="80" Cursor="Hand"
                           Style="{StaticResource TextBlockStyle}"/>
            </StackPanel>
            <Image Source="{Binding RightImage}" Height="300" Width="300" Margin="50,-50,50,50"/>
        </WrapPanel>
    </Grid>
</UserControl>
