<UserControl x:Class="ScreenplayClassifier.MVVM.Views.BrowseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
             Style="{StaticResource UserControlStyle}">
    <Grid>
        <StackPanel x:Name="BrowseStackPanel" HorizontalAlignment="Center" Visibility="Visible">
            <TextBlock Text="Browse Screenplays" HorizontalAlignment="Center" FontSize="40" Margin="0,10,0,10"
                       Style="{StaticResource TextBlockStyle}"/>
            <WrapPanel HorizontalAlignment="Center" Margin="0,10,0,10">
                <Button Command="{Binding BrowseScreenplaysCommand}" Height="100" Width="100" IsEnabled="{Binding CanBrowse}" ToolTip="Browse..."
                        Style="{StaticResource ButtonStyle}">
                    <Button.Content>
                        <Image Source="/Media/Images/Browse.png"/>
                    </Button.Content>
                </Button>
                <Button Command="{Binding ClearScreenplaysCommand}" Height="100" Width="100" IsEnabled="{Binding CanClear}" ToolTip="Clear"
                        Margin="10,0,0,0" Style="{StaticResource ButtonStyle}">
                    <Button.Content>
                        <Image Source="/Media/Images/Clear.png"/>
                    </Button.Content>
                </Button>
            </WrapPanel>
            <DataGrid x:Name="BrowsedScreenplaysDataGrid" Height="400" FontSize="20" CanUserAddRows="False" IsEnabled="{Binding CanBrowse}"
                      SelectedIndex="{Binding SelectedBrowsedScreenplay}" ItemsSource="{Binding BrowsedScreenplays}"
                      Margin="10" Style="{StaticResource DataGridStyle}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyDown">
                        <i:InvokeCommandAction Command="{Binding CheckKeyCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Width="390" CanUserResize="False" IsReadOnly="True" Binding="{Binding Path=Name}"
                                        HeaderStyle="{StaticResource DataGridMainColumnHeaderStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Command="{Binding ProceedToClassificationCommand}" Height="110" Width="110" IsEnabled="{Binding CanProceed}"
                    ToolTip="Proceed to classification" Style="{StaticResource ButtonStyle}">
                <Button.Content>
                    <Image Source="/Media/Images/PlayUnpressed.png"/>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
