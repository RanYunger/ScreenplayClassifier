<UserControl x:Class="ScreenplayClassifier.MVVM.Views.ClassificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
             Style="{StaticResource UserControlStyle}">
    <Grid>
        <TabControl x:Name="TabControl" TabStripPlacement="Left">
            <TabItem x:Name="BrowseTabItem" Header="Browse Screenplays" Style="{StaticResource TabItemStyle}">
                <v:BrowseView x:Name="BrowseView"/>
            </TabItem>
            <TabItem x:Name="ProgressTabItem" Header="View Progress" Visibility="Collapsed" Style="{StaticResource TabItemStyle}">
                <v:ProgressView x:Name="ProgressView"/>
            </TabItem>
            <TabItem x:Name="ResultsTabItem" Header="View Classifications" Visibility="Collapsed" Style="{StaticResource TabItemStyle}">
                <v:ResultsView x:Name="ResultsView"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
