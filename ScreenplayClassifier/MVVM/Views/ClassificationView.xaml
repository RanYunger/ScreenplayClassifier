<UserControl x:Class="ScreenplayClassifier.MVVM.Views.ClassificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScreenplayClassifier.MVVM.Views"
             mc:Ignorable="d" 
             MinHeight="500" MinWidth="1200" Style="{StaticResource UserControlStyle}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="238"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TabControl Grid.Column="0" AllowDrop="False">
            <TabControl.Items>
                <TabItem Header="Step 1" FontSize="40" Style="{StaticResource TabItemStyle}"/>
                <TabItem Header="Step 2" FontSize="40" Style="{StaticResource TabItemStyle}"/>
                <TabItem Header="Step 3" FontSize="40" Style="{StaticResource TabItemStyle}"/>
            </TabControl.Items>
        </TabControl>
    </Grid>
</UserControl>
