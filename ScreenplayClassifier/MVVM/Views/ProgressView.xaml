<UserControl x:Class="ScreenplayClassifier.MVVM.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
             Style="{StaticResource UserControlStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="{Binding ClassificationsText}" FontSize="30" Margin="20,0,0,0" Style="{StaticResource TextBlockStyle}"/>
            <TextBlock Text="{Binding DurationText}" FontSize="30" Margin="680,0,0,0" Style="{StaticResource TextBlockStyle}"/>
        </WrapPanel>
        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="/Media/Images/Dial.png" Height="600" Width="600" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Ellipse Height="390" Width="390" Stroke="#4D4D4D" StrokeThickness="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <v:CircularProgressBarView x:Name="CircularProgressBar" Margin="125,400,0,0"/>
            <TextBlock Text="0" Height="100" Width="100" FontSize="50" Foreground="#FEF200" TextAlignment="Center"
                       Style="{StaticResource TextBlockStyle}"/>
            <TextBlock Text="P E R C E N T" Height="20" Width="210" FontSize="15" TextAlignment="Center" Margin="0,60,0,0"
                       Style="{StaticResource TextBlockStyle}"/>
        </Grid>
    </Grid>
</UserControl>
