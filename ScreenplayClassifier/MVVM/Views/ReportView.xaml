<Window x:Class="ScreenplayClassifier.MVVM.Views.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
        Title="Screenplay Classifier" Height="800" Width="800" ResizeMode="NoResize" Style="{StaticResource WindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Classification Report" Grid.Row="0" Grid.ColumnSpan="4" HorizontalAlignment="Center" FontSize="40" Margin="10"
                   Style="{StaticResource TextBlockStyle}"/>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <TextBlock Text="Overview" FontSize="30" HorizontalAlignment="Center" Style="{StaticResource TextBlockStyle}"/>
            <WrapPanel Margin="10">
                <TextBlock Text="Owner:" Style="{StaticResource TextBlockStyle}"/>
                <TextBox Text="{Binding ClassificationReport.Owner.Username}" Width="300" Margin="20,0,0,0"
                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
            </WrapPanel>
            <WrapPanel Margin="10">
                <TextBlock Text="Name:" Style="{StaticResource TextBlockStyle}"/>
                <TextBox Text="{Binding ClassificationReport.Screenplay.Name}" Width="300" Margin="25,0,0,0"
                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
            </WrapPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Genres" FontSize="30" HorizontalAlignment="Center" Style="{StaticResource TextBlockStyle}"/>
            <v:GenresView x:Name="GenresView" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
