<Window x:Class="ScreenplayClassifier.MVVM.Views.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:ScreenplayClassifier.MVVM.Views"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Screenplay Classifier" Height="650" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Style="{StaticResource WindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.RowSpan="2" Grid.ColumnSpan="2" Source="/Media/Images/Document.png" Opacity="0.3"/>
        <TextBlock Text="Classification" Grid.Row="0" Grid.ColumnSpan="4" HorizontalAlignment="Center" FontSize="80" Margin="10"
                   Style="{StaticResource TextBlockStyle}"/>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <TextBlock Text="Overview" FontSize="30" HorizontalAlignment="Center" Style="{StaticResource TextBlockStyle}"/>
            <WrapPanel Margin="10">
                <TextBlock Text="Owner:" Style="{StaticResource TextBlockStyle}"/>
                <TextBox Text="{Binding ClassificationReport.Owner.Username}" Width="300" Margin="20,0,0,0"
                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
            </WrapPanel>
            <WrapPanel Margin="10">
                <TextBlock Text="Title:" Style="{StaticResource TextBlockStyle}"/>
                <TextBox Text="{Binding ClassificationReport.Screenplay.Title}" Width="300" Margin="35,0,0,0"
                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
            </WrapPanel>
            <TextBlock Text="Genre Predictions" FontSize="20" HorizontalAlignment="Center" Margin="10" Style="{StaticResource TextBlockStyle}"/>
            <lvc:PieChart LegendLocation="Bottom" Series="{Binding PercentageSeries}" Height="300" Width="300" Hoverable="True" Margin="20">
                <lvc:PieChart.ChartLegend>
                    <lvc:DefaultLegend BulletSize="20" TextBlock.FontSize="10"/>
                </lvc:PieChart.ChartLegend>
            </lvc:PieChart>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Actual Genres" FontSize="30" HorizontalAlignment="Center" Style="{StaticResource TextBlockStyle}"/>
            <v:GenresView x:Name="GenresView" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
